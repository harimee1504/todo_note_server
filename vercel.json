{
    "version": 2,
    "functions": {
        "api/**/*.ts": {
            "includeFiles": "api/graphql/**/*.graphql"
        }
    },
    "routes": [
        {
            "src": "/(.*)",
            "dest": "api/index.ts"
        }
    ]
}